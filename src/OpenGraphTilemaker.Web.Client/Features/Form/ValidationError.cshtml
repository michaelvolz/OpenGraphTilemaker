@inherits ValidationErrorModel
@if (HasValidationFailures) {
    <div class="text-danger">
        <ul>
            @foreach (var failure in ValidationFailures) {
                <li>@failure</li>
            }
        </ul>
    </div>
}