@page "/counter"
@inherits CounterModel

<h1>Counter</h1>

<p>Current count: @CounterState.Count</p>

<button class="button primary shadow radius" onclick="@(async () => await ButtonClickAsync())">Click me</button>