@page "/counter"
@inherits CounterModel

<h1>Counter</h1>

<p>Current count: @CounterState.Count</p>

<button class="btn btn-primary" onclick="@(async () => await ButtonClickAsync())">Click me</button>